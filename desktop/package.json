{
  "name": "minicars-control-station-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MiniCars Control Station \u2013 dashboard local para controlar el stream de la Jetson y el auto RC.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ci": "vite build",
    "preview": "vite preview",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run check-env && tauri build",
    "tauri:build:ci": "tauri build",
    "tauri": "tauri dev",
    "check-env": "powershell -ExecutionPolicy Bypass -File ../scripts/check-windows-env.ps1"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.3",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
