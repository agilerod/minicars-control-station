# Configuración del frontend MiniCars (Tauri + Vite + React)
# Copia este archivo a .env y ajusta los valores según tu entorno

# URL base del backend FastAPI
VITE_MINICARS_BACKEND_URL=http://127.0.0.1:8000
