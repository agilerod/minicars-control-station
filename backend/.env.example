# Configuración del backend MiniCars
# Copia este archivo a .env y ajusta los valores según tu entorno

# Entorno de ejecución (dev, prod, etc.)
MINICARS_ENV=dev

# Host y puerto donde se ejecuta el servidor FastAPI
MINICARS_BACKEND_HOST=127.0.0.1
MINICARS_BACKEND_PORT=8000

# URL base pública del backend (usada por el frontend y otros clientes)
MINICARS_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Joystick / Car Control
# Hostname o IP de la Jetson Nano para comandos de joystick
MINICARS_JOYSTICK_TARGET_HOST=SKLNx.local
MINICARS_JOYSTICK_TARGET_PORT=5005
MINICARS_JOYSTICK_SEND_HZ=20
MINICARS_JOYSTICK_RECONNECT_DELAY=2.0

# Opcional, para cuando tengamos Jetson API REST:
# URL base del backend/control que corre en la Jetson Nano
# MINICARS_JETSON_BASE_URL=http://192.168.0.50:9000
